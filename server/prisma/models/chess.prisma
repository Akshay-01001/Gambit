model ChessProfile {
  id String @id @default(cuid())

  userId String @unique
  user   User   @relation(fields: [userId], references: [id], onDelete: Cascade)

  totalGames Int @default(0)

  totalGamesWon  Int @default(0)
  totalGamesLost Int @default(0)
  totalGamesDraw Int @default(0)

  totalWhiteWins Int @default(0)
  totalBlackWins Int @default(0)

  blitzRating     Int @default(100)
  rapidRating     Int @default(100)
  classicalRating Int @default(100)
  bulletRating    Int @default(100)

  highestRating Int @default(100)

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
